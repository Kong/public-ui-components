import './chart.scss'
import './mixins.scss'

import { describe, it, expect } from 'vitest'

describe('chart styles', () => {
  it('compile without unresolved design token variables', () => {
    expect(true).toBe(true)
  })
})
