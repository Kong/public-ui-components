# Compatibility in the Vault entity

## Name

`'env'` is the only valid values for the `name` field in Gateway Community Edition:
- In the `VaultForm` component, vault provider options other than `'env'` are disabled.