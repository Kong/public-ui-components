import type { MockingSchema } from '../../types/plugins/mocking'

export const mockingSchema: MockingSchema = {
  'config-api_specification': {
    label: 'API Specification',
    placeholder: 'Enter an API spec',
    type: 'textArea',
    rows: 10,
  },
}
